{
	"extends": "./tsconfig.json",
	"compilerOptions": {
		"noEmit": false,
		"declaration": true,
		"declarationMap": true,
		"sourceMap": true,
		"outDir": "dist",
		"rootDir": "src",
		// Our source imports carry .ts extensions so the pure modules run under bare Node. This
		// rewrites them to .js in the emitted JS. Declarations need the sed in build:package.
		"rewriteRelativeImportExtensions": true
	},
	"include": ["src"]
}
